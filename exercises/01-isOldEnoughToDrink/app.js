function isOldEnoughToDrink(currentAge){
    // your code here	
    if (currentAge >= 21) {
        return true;
    } else {
        return false;
    }
}

var output = isOldEnoughToDrink(22);
isOldEnoughToDrink(output);